# ============================================
# .bashrc
# ============================================

# 対話シェルチェック
case $- in
    *i*) ;;
      *) return;;
esac

# シェル設定を読み込み
for f in ~/.config/shell/{common,bash}.sh; do
    [ -r "$f" ] && . "$f"
done
